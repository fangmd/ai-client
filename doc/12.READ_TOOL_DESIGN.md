# 文件读取工具 (Read Tool) 设计方案

## 一、背景

### 1.1 功能目标

实现一个文件读取工具，允许 AI 在对话中读取本地文件系统的文件内容。AI 可以调用此工具读取代码文件、配置文件、文档等，获取文件内容，并基于内容继续对话。

### 1.2 核心价值

- **增强 AI 能力**：让 AI 能够直接读取和分析本地文件内容
- **提高效率**：用户可以通过自然语言让 AI 读取和分析文件
- **安全可控**：通过路径限制和权限控制确保安全性
- **支持多种文件类型**：支持文本文件、图片文件、PDF 文件等

### 1.3 技术选型

- **执行环境**：Node.js `fs` 模块
- **安全机制**：路径限制、二进制文件检测、超时控制
- **集成方式**：Function Calling（Chat Completions API）或自定义工具（Responses API）

## 二、方案设计

### 2.1 核心思路

文件读取工具作为本地工具，需要在客户端读取文件并返回内容。与 OpenAI 内置工具（如 `web_search`）不同，文件读取工具的执行完全在本地进行。

```
用户消息
  ↓
AI 决定调用文件读取工具
  ↓
工具调用消息（执行中）
  ↓
读取文件内容（本地）
  ↓
工具调用消息（完成，显示结果）
  ↓
AI 基于结果继续回复
```

### 2.2 技术方案对比

#### 方案A：Function Calling（推荐）

**适用场景**：Chat Completions API 和 Responses API 都支持

**优点**：
- OpenAI 标准 Function Calling 机制
- 支持所有兼容 Function Calling 的模型
- 工具定义清晰（JSON Schema）
- 支持多工具调用
- Responses API 和 Chat Completions API 都支持

**采用方案：方案A（Function Calling）**

**理由**：
1. 统一使用 Function Calling 机制，代码更简洁
2. 同时支持 Responses API 和 Chat Completions API
3. 标准化的工具定义，易于维护和扩展
4. 与现有架构兼容性好（与 terminal tool 保持一致）

## 三、架构设计

### 3.1 分层架构

```
┌─────────────────────────────────────────┐
│         Renderer 层 (UI)               │
│  - 工具调用消息显示                      │
│  - 文件内容展示                          │
└──────────────┬──────────────────────────┘
               │ IPC 通信
┌──────────────▼──────────────────────────┐
│         IPC 层 (通信桥梁)                │
│  - 工具调用事件                          │
│  - 文件读取结果                          │
└──────────────┬──────────────────────────┘
               │ IPC 调用
┌──────────────▼──────────────────────────┐
│         Handler 层                       │
│  - AIHandler: 处理工具调用                │
│  - FileReader: 读取文件内容               │
└──────────────┬──────────────────────────┘
               │ 调用
┌──────────────▼──────────────────────────┐
│         Provider 层                      │
│  - OpenAIProvider: Function Calling      │
│  - 工具定义和调用处理                     │
└──────────────┬──────────────────────────┘
               │ 执行
┌──────────────▼──────────────────────────┐
│         File Reader 层                   │
│  - 文件读取                              │
│  - 安全验证                              │
│  - 内容处理                              │
└─────────────────────────────────────────┘
```

### 3.2 设计模式

- **策略模式**：不同 AI Provider 使用不同的工具调用机制
- **责任链模式**：文件路径安全验证链

## 四、类型定义

### 4.1 工具类型扩展

```typescript
// src/main/providers/index.ts

// 扩展工具类型
export type ToolType = 'web_search' | 'file_search' | 'terminal' | 'read'

// 文件读取工具调用信息
export interface ReadToolCallInfo {
  itemId: string              // 工具调用的唯一标识
  type: 'read'                // 工具类型
  status: ToolCallStatus      // 当前状态
  filePath?: string           // 读取的文件路径（完成时才有）
  offset?: number             // 起始行号
  limit?: number              // 读取行数
  outputIndex?: number        // 在输出中的索引位置
  timestamp?: number          // 时间戳
}

// 扩展 ToolCallInfo（联合类型）
export type ToolCallInfo = 
  | { type: 'web_search' | 'file_search'; /* ... */ }
  | TerminalToolCallInfo
  | ReadToolCallInfo
```

### 4.2 Function Calling 工具定义

```typescript
// src/main/providers/tools/read-tool.ts

/**
 * Responses API 格式的文件读取工具定义
 */
export const readToolDefinitionForResponsesAPI = {
  type: 'function' as const,
  name: 'read',
  description: `Reads a file from the local filesystem. You can access any file directly by using this tool.
Assume this tool is able to read all files on the machine. If the User provides a path to a file assume that path is valid. It is okay to read a file that does not exist; an error will be returned.

Usage:
- The filePath parameter must be an absolute path, not a relative path
- By default, it reads up to 2000 lines starting from the beginning of the file
- You can optionally specify a line offset and limit (especially handy for long files), but it's recommended to read the whole file by not providing these parameters
- Any lines longer than 2000 characters will be truncated
- Results are returned using cat -n format, with line numbers starting at 1
- You have the capability to call multiple tools in a single response. It is always better to speculatively read multiple files as a batch that are potentially useful.
- If you read a file that exists but has empty contents you will receive a system reminder warning in place of file contents.
- You can read image files using this tool.`,
  parameters: {
    type: 'object',
    properties: {
      filePath: {
        type: 'string',
        description: 'The absolute path to the file to read'
      },
      offset: {
        type: 'number',
        description: 'The line number to start reading from (0-based). Optional, defaults to 0.'
      },
      limit: {
        type: 'number',
        description: 'The number of lines to read (defaults to 2000). Optional.'
      }
    },
    required: ['filePath']
  }
}

/**
 * 默认导出（为了向后兼容，使用 Responses API 格式）
 */
export const readToolDefinition = readToolDefinitionForResponsesAPI
```

### 4.3 文件读取结果类型

```typescript
// src/main/utils/file-reader.ts

export interface FileReadResult {
  success: boolean
  filePath: string
  title: string              // 相对路径（用于显示）
  content?: string           // 文本文件内容
  mimeType?: string          // MIME 类型
  size?: number              // 文件大小（字节）
  totalLines?: number        // 总行数
  readLines?: number         // 已读取行数
  hasMore?: boolean          // 是否还有更多内容
  preview?: string           // 预览内容（前20行）
  attachments?: Array<{     // 附件（图片、PDF等）
    id: string
    type: 'file'
    mime: string
    url: string              // base64 data URL
  }>
  error?: string             // 错误信息（如果有）
}
```

## 五、核心实现

### 5.1 File Reader 实现

```typescript
// src/main/utils/file-reader.ts

import * as fs from 'fs'
import * as path from 'path'
import { promisify } from 'util'
import { logInfo, logError, logWarn, logDebug } from './logger'
import * as os from 'os'
import { app } from 'electron'

const readFileAsync = promisify(fs.readFile)
const statAsync = promisify(fs.stat)
const readdirAsync = promisify(fs.readdir)

/**
 * 默认读取行数限制
 */
const DEFAULT_READ_LIMIT = 2000

/**
 * 最大单行长度（字符）
 */
const MAX_LINE_LENGTH = 2000

/**
 * 获取项目根目录（工作目录）
 */
function getProjectRoot(): string {
  // 开发模式下使用当前工作目录
  if (!app.isPackaged) {
    return process.cwd()
  }
  // 生产模式下使用 userData 目录的父目录，或当前工作目录
  try {
    const userDataPath = app.getPath('userData')
    // 返回 userData 的父目录，或使用当前工作目录
    return path.dirname(userDataPath) || process.cwd()
  } catch {
    return process.cwd()
  }
}

/**
 * 验证路径是否允许访问
 */
function isPathAllowed(filePath: string, bypassCwdCheck: boolean = false): boolean {
  const resolvedPath = path.resolve(filePath)
  const homeDir = os.homedir()
  const projectRoot = getProjectRoot()
  
  // 如果启用 bypassCwdCheck，允许访问所有路径
  if (bypassCwdCheck) {
    return true
  }
  
  // 允许访问用户目录及其子目录
  if (resolvedPath.startsWith(homeDir)) {
    return true
  }
  
  // 允许访问项目目录及其子目录
  if (resolvedPath.startsWith(projectRoot)) {
    return true
  }
  
  // 允许访问临时目录
  if (resolvedPath.startsWith(os.tmpdir())) {
    return true
  }
  
  return false
}

/**
 * 检查文件是否为二进制文件
 */
async function isBinaryFile(filePath: string, file: Buffer): Promise<boolean> {
  const ext = path.extname(filePath).toLowerCase()
  
  // 二进制文件扩展名列表
  const binaryExtensions = [
    '.zip', '.tar', '.gz', '.exe', '.dll', '.so', '.class',
    '.jar', '.war', '.7z', '.doc', '.docx', '.xls', '.xlsx',
    '.ppt', '.pptx', '.odt', '.ods', '.odp', '.bin', '.dat',
    '.obj', '.o', '.a', '.lib', '.wasm', '.pyc', '.pyo'
  ]
  
  if (binaryExtensions.includes(ext)) {
    return true
  }
  
  // 检查文件大小
  const fileSize = file.length
  if (fileSize === 0) {
    return false
  }
  
  // 读取前 4096 字节检查
  const bufferSize = Math.min(4096, fileSize)
  const bytes = file.slice(0, bufferSize)
  
  // 检查是否包含 null 字节
  if (bytes.includes(0)) {
    return true
  }
  
  // 检查非打印字符比例
  let nonPrintableCount = 0
  for (let i = 0; i < bytes.length; i++) {
    const byte = bytes[i]
    // 允许制表符(9)、换行符(10)、回车符(13)
    if (byte < 9 || (byte > 13 && byte < 32)) {
      nonPrintableCount++
    }
  }
  
  // 如果超过 30% 是非打印字符，认为是二进制文件
  return nonPrintableCount / bytes.length > 0.3
}

/**
 * 获取文件的 MIME 类型
 */
function getMimeType(filePath: string): string {
  const ext = path.extname(filePath).toLowerCase()
  const mimeTypes: Record<string, string> = {
    '.txt': 'text/plain',
    '.js': 'text/javascript',
    '.ts': 'text/typescript',
    '.jsx': 'text/javascript',
    '.tsx': 'text/typescript',
    '.json': 'application/json',
    '.html': 'text/html',
    '.css': 'text/css',
    '.md': 'text/markdown',
    '.py': 'text/x-python',
    '.java': 'text/x-java-source',
    '.c': 'text/x-c',
    '.cpp': 'text/x-c++',
    '.h': 'text/x-c',
    '.hpp': 'text/x-c++',
    '.go': 'text/x-go',
    '.rs': 'text/x-rust',
    '.php': 'text/x-php',
    '.rb': 'text/x-ruby',
    '.sh': 'text/x-shellscript',
    '.yaml': 'text/yaml',
    '.yml': 'text/yaml',
    '.xml': 'text/xml',
    '.svg': 'image/svg+xml',
    '.png': 'image/png',
    '.jpg': 'image/jpeg',
    '.jpeg': 'image/jpeg',
    '.gif': 'image/gif',
    '.webp': 'image/webp',
    '.pdf': 'application/pdf'
  }
  
  return mimeTypes[ext] || 'application/octet-stream'
}

/**
 * 读取文件内容
 */
export async function readFile(
  filePath: string,
  options?: {
    offset?: number
    limit?: number
    bypassCwdCheck?: boolean
  }
): Promise<FileReadResult> {
  const startTime = Date.now()
  const { offset = 0, limit = DEFAULT_READ_LIMIT, bypassCwdCheck = false } = options || {}
  
  let resolvedPath = filePath
  
  // 处理相对路径：转换为绝对路径
  if (!path.isAbsolute(filePath)) {
    resolvedPath = path.join(getProjectRoot(), filePath)
  }
  
  resolvedPath = path.resolve(resolvedPath)
  
  // 计算相对路径（用于显示）
  const projectRoot = getProjectRoot()
  const title = path.relative(projectRoot, resolvedPath)
  
  logInfo('Reading file', {
    filePath: resolvedPath,
    title,
    offset,
    limit,
    bypassCwdCheck
  })
  
  // 1. 路径安全验证
  if (!isPathAllowed(resolvedPath, bypassCwdCheck)) {
    const error = `Access denied: File path "${resolvedPath}" is not allowed`
    logWarn('File access denied', { filePath: resolvedPath })
    return {
      success: false,
      filePath: resolvedPath,
      title,
      error
    }
  }
  
  try {
    // 2. 检查文件是否存在
    const stats = await statAsync(resolvedPath)
    if (!stats.isFile()) {
      const error = `Path "${resolvedPath}" is not a file`
      logWarn('Path is not a file', { filePath: resolvedPath })
      return {
        success: false,
        filePath: resolvedPath,
        title,
        error
      }
    }
    
    // 3. 读取文件
    const fileBuffer = await readFileAsync(resolvedPath)
    const mimeType = getMimeType(resolvedPath)
    const size = fileBuffer.length
    
    // 4. 处理图片和 PDF 文件
    const isImage = mimeType.startsWith('image/') && mimeType !== 'image/svg+xml'
    const isPdf = mimeType === 'application/pdf'
    
    if (isImage || isPdf) {
      const base64 = fileBuffer.toString('base64')
      const dataUrl = `data:${mimeType};base64,${base64}`
      
      logInfo('Image/PDF file read successfully', {
        filePath: resolvedPath,
        mimeType,
        size
      })
      
      return {
        success: true,
        filePath: resolvedPath,
        title,
        mimeType,
        size,
        attachments: [
          {
            id: `read-${Date.now()}`,
            type: 'file',
            mime: mimeType,
            url: dataUrl
          }
        ],
        content: `${isImage ? 'Image' : 'PDF'} read successfully`
      }
    }
    
    // 5. 检查二进制文件
    const isBinary = await isBinaryFile(resolvedPath, fileBuffer)
    if (isBinary) {
      const error = `Cannot read binary file: ${resolvedPath}`
      logWarn('Binary file detected', { filePath: resolvedPath })
      return {
        success: false,
        filePath: resolvedPath,
        title,
        mimeType,
        size,
        error
      }
    }
    
    // 6. 处理文本文件
    const text = fileBuffer.toString('utf-8')
    const lines = text.split('\n')
    const totalLines = lines.length
    
    // 截取指定范围的行
    const startLine = Math.max(0, offset)
    const endLine = Math.min(startLine + limit, totalLines)
    const readLines = lines.slice(startLine, endLine)
    
    // 截断过长的行
    const truncatedLines = readLines.map((line) => {
      return line.length > MAX_LINE_LENGTH
        ? line.substring(0, MAX_LINE_LENGTH) + '...'
        : line
    })
    
    // 格式化输出（带行号）
    const content = truncatedLines.map((line, index) => {
      const lineNumber = startLine + index + 1
      return `${lineNumber.toString().padStart(5, '0')}| ${line}`
    })
    
    // 生成预览（前20行）
    const preview = truncatedLines.slice(0, 20).join('\n')
    
    // 构建输出
    let output = '<file>\n'
    output += content.join('\n')
    
    const hasMore = totalLines > endLine
    if (hasMore) {
      output += `\n\n(File has more lines. Use 'offset' parameter to read beyond line ${endLine})`
    } else {
      output += `\n\n(End of file - total ${totalLines} lines)`
    }
    output += '\n</file>'
    
    const executionTime = Date.now() - startTime
    
    logInfo('File read successfully', {
      filePath: resolvedPath,
      totalLines,
      readLines: readLines.length,
      hasMore,
      executionTime
    })
    
    return {
      success: true,
      filePath: resolvedPath,
      title,
      content: output,
      mimeType,
      size,
      totalLines,
      readLines: readLines.length,
      hasMore,
      preview
    }
  } catch (error: any) {
    // 文件不存在，提供建议
    if (error.code === 'ENOENT') {
      const dir = path.dirname(resolvedPath)
      const base = path.basename(resolvedPath)
      
      try {
        const dirEntries = await readdirAsync(dir)
        const suggestions = dirEntries
          .filter((entry) => {
            const entryLower = entry.toLowerCase()
            const baseLower = base.toLowerCase()
            return entryLower.includes(baseLower) || baseLower.includes(entryLower)
          })
          .map((entry) => path.join(dir, entry))
          .slice(0, 3)
        
        let errorMsg = `File not found: ${resolvedPath}`
        if (suggestions.length > 0) {
          errorMsg += `\n\nDid you mean one of these?\n${suggestions.join('\n')}`
        }
        
        logWarn('File not found with suggestions', {
          filePath: resolvedPath,
          suggestions
        })
        
        return {
          success: false,
          filePath: resolvedPath,
          title,
          error: errorMsg
        }
      } catch (dirError) {
        // 如果无法读取目录，返回简单错误
        logError('Failed to read directory for suggestions', dirError)
      }
    }
    
    const errorMsg = error.message || 'Unknown error occurred'
    logError('File read failed', {
      filePath: resolvedPath,
      error: errorMsg,
      errorCode: error.code
    })
    
    return {
      success: false,
      filePath: resolvedPath,
      title,
      error: errorMsg
    }
  }
}
```

### 5.2 Provider 层集成（Function Calling）

参考 terminal tool 的实现方式，在 `OpenAIProvider` 中添加 read tool 的处理逻辑：

```typescript
// src/main/providers/openai-provider.ts

// 在 streamChatWithResponsesAPI 中添加 read tool 支持
// 1. 在工具列表中添加 read tool 定义
if (tools.includes('read')) {
  toolsList.push(readToolDefinition)
}

// 2. 处理 function_call 事件
case 'response.output_item.added': {
  const event = chunk as any
  if (event.item?.type === 'function_call') {
    const functionName = event.item.name
    if (functionName === 'read') {
      // 创建 read tool call info
      const toolInfo: ReadToolCallInfo = {
        itemId: event.item.id,
        type: 'read',
        status: 'in_progress',
        outputIndex: event.output_index,
        timestamp: Date.now()
      }
      toolCallsMap.set(event.item.id, toolInfo)
      functionCallArgsMap.set(event.item.id, '')
      callbacks.onToolCallStart?.(toolInfo)
    }
  }
  break
}

// 3. 处理 function_call 完成事件
case 'response.output_item.done': {
  const event = chunk as any
  if (event.item?.type === 'function_call') {
    const functionName = event.item.name
    if (functionName === 'read') {
      await handleFunctionCall(
        {
          name: functionName,
          arguments: functionCallArgsMap.get(event.item.id) || event.item.arguments || '',
          callId: event.item.call_id || event.item.id,
          functionCallItem: event.item
        },
        toolInfo,
        messages,
        config,
        callbacks,
        abortSignal,
        options
      )
    }
  }
  break
}

// 4. 在 handleFunctionCall 中添加 read 处理
private async handleFunctionCall(
  functionCall: {
    name: string
    arguments: string
    callId: string
    functionCallItem?: any
  },
  toolInfo: ToolCallInfo,
  messages: AIMessageInput[],
  config: AIConfig,
  callbacks: StreamCallbacks,
  abortSignal?: AbortSignal,
  options?: { tools?: ToolType[] }
): Promise<void> {
  if (functionCall.name === 'read') {
    try {
      const args = JSON.parse(functionCall.arguments)
      const { filePath, offset, limit } = args
      
      // 执行文件读取
      const result = await readFile(filePath, { offset, limit })
      
      // 更新 toolInfo
      if (toolInfo.type === 'read') {
        toolInfo.filePath = result.filePath
        toolInfo.offset = offset
        toolInfo.limit = limit
      }
      
      // 格式化结果
      let formattedResult: string
      if (result.success) {
        if (result.attachments && result.attachments.length > 0) {
          // 图片或 PDF 文件
          formattedResult = result.content || `${result.mimeType} file read successfully`
        } else {
          // 文本文件
          formattedResult = result.content || ''
        }
      } else {
        formattedResult = `Error: ${result.error}`
      }
      
      // 通知工具调用完成
      callbacks.onToolCallComplete?.(toolInfo, formattedResult)
      
      // 构建 function_call_output（Responses API 格式）
      const functionCallOutput = {
        type: 'function_call_output' as const,
        call_id: functionCall.callId,
        output: formattedResult
      }
      
      // 如果有附件，需要特殊处理
      if (result.attachments && result.attachments.length > 0) {
        // 对于图片和 PDF，需要将附件信息也传递
        // 这里可以根据需要扩展
      }
      
      // 将 function_call_output 添加到消息列表
      messages.push({
        role: 'user',
        content: JSON.stringify(functionCallOutput),
        _functionCallOutput: functionCallOutput
      } as any)
      
      // 递归调用，让 AI 基于结果继续回复
      await this.streamChat(messages, config, callbacks, abortSignal, options)
    } catch (error) {
      logError('Function call execution failed', error)
      callbacks.onError(error instanceof Error ? error : new Error('Function call failed'))
    }
  }
  // ... terminal tool 处理逻辑 ...
}
```

### 5.3 Handler 层集成

Handler 层无需额外处理，工具调用消息的创建和更新与 terminal tool 类似，通过 Provider 层的回调自动处理。

## 六、数据库 Schema

文件读取工具调用使用现有的 Message 表结构，通过 `toolType` 字段区分：

```prisma
// prisma/schema.prisma

model Message {
  // ... 现有字段 ...
  
  toolType        String?  // web_search | file_search | terminal | read
  toolStatus      String?  // in_progress | completed | failed
  toolQuery       String?  // 工具查询内容（对于 read，存储文件路径）
  toolItemId      String?  // 工具调用的 item_id
  toolOutputIndex Int?     // 工具在输出中的索引
  
  // ... 其他字段 ...
}
```

## 七、前端 UI 实现

### 7.1 工具调用消息组件扩展

```typescript
// src/renderer/src/chat/tool-call-item.tsx

export function ToolCallItem({ message }: ToolCallItemProps) {
  const { toolType, toolQuery, toolStatus } = message
  
  // 文件读取工具特殊处理
  if (toolType === 'read') {
    return <ReadToolCallItem message={message} />
  }
  
  // 其他工具（web_search, file_search, terminal）
  // ... 现有逻辑 ...
}

/**
 * 文件读取工具调用组件
 */
function ReadToolCallItem({ message }: { message: DbMessageWithAttachments }) {
  const filePath = message.toolQuery || ''
  const status = message.toolStatus || 'in_progress'
  
  return (
    <div className="tool-call-item bg-gray-50 dark:bg-gray-800 rounded-lg p-4 my-2 border border-gray-200 dark:border-gray-700">
      <div className="flex items-center gap-2 mb-2">
        <span className="text-lg">📄</span>
        <span className="font-medium text-gray-900 dark:text-gray-100">
          读取文件
        </span>
        <div className="flex items-center gap-1.5 ml-auto">
          {getStatusIcon(status)}
          <span className="text-sm text-gray-500 dark:text-gray-400">
            {getStatusText(status)}
          </span>
        </div>
      </div>
      
      {filePath && (
        <div className="mt-2">
          <div className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
            文件路径：
          </div>
          <div className="bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-200 font-mono text-sm p-2 rounded overflow-x-auto">
            {filePath}
          </div>
        </div>
      )}
      
      {status === 'completed' && message.content && (
        <div className="mt-2">
          <div className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
            内容：
          </div>
          <div className="bg-gray-900 text-gray-100 font-mono text-xs p-3 rounded overflow-x-auto max-h-96 overflow-y-auto">
            <pre className="whitespace-pre-wrap">{message.content}</pre>
          </div>
        </div>
      )}
      
      {status === 'failed' && message.content && (
        <div className="mt-2">
          <div className="text-sm font-medium text-red-700 dark:text-red-300 mb-1">
            错误：
          </div>
          <div className="bg-red-50 dark:bg-red-900/20 text-red-800 dark:text-red-200 text-sm p-2 rounded">
            {message.content}
          </div>
        </div>
      )}
    </div>
  )
}
```

## 八、安全机制

### 8.1 路径限制

- **默认限制**：只允许访问用户目录（`~`）、项目目录和临时目录
- **可配置**：支持 `bypassCwdCheck` 选项绕过路径检查（用于特殊场景）
- **路径解析**：正确处理相对路径和绝对路径

### 8.2 二进制文件检测

- **扩展名检测**：检测常见二进制文件扩展名
- **内容检测**：检查文件内容中的 null 字节和非打印字符比例
- **友好错误**：对二进制文件返回清晰的错误提示

### 8.3 资源限制

- **行数限制**：默认读取 2000 行，可通过参数调整
- **单行长度限制**：单行超过 2000 字符时截断
- **文件大小限制**：通过 Node.js Buffer 限制（默认无限制，但建议添加）

### 8.4 错误处理

- **文件不存在**：提供相似文件名建议
- **权限错误**：返回清晰的权限错误提示
- **读取失败**：捕获并返回友好的错误信息

## 九、实现步骤

### 阶段一：基础实现

- [ ] **步骤1：类型定义扩展**
  - [ ] 扩展 `ToolType`，添加 `'read'`
  - [ ] 定义 `ReadToolCallInfo` 接口
  - [ ] 定义 `FileReadResult` 接口

- [ ] **步骤2：File Reader 实现**
  - [ ] 实现 `readFile` 函数
  - [ ] 实现路径验证逻辑
  - [ ] 实现二进制文件检测
  - [ ] 实现文本文件读取和格式化
  - [ ] 实现图片和 PDF 文件处理
  - [ ] 实现文件不存在时的建议功能

- [ ] **步骤3：工具定义**
  - [ ] 定义 Function Calling 工具 Schema
  - [ ] 实现工具描述和参数定义

### 阶段二：Provider 集成

- [ ] **步骤4：Chat Completions API 集成**
  - [ ] 在 `streamChatWithCompletionsAPI` 中添加工具支持
  - [ ] 实现 Function Calling 处理逻辑
  - [ ] 实现工具调用结果处理
  - [ ] 实现递归调用（工具结果后继续对话）

- [ ] **步骤5：Responses API 集成**
  - [ ] 在 `streamChatWithResponsesAPI` 中添加 Function Calling 支持
  - [ ] 处理 Responses API 的 Function Calling 事件
  - [ ] 实现与内置工具（web_search）的兼容处理
  - [ ] 实现工具调用结果处理

- [ ] **步骤6：工具调用消息管理**
  - [ ] 在 Handler 层处理工具调用消息创建
  - [ ] 实现工具调用状态更新
  - [ ] 实现工具调用完成处理

### 阶段三：UI 实现

- [ ] **步骤7：前端组件**
  - [ ] 创建 `ReadToolCallItem` 组件
  - [ ] 实现文件路径显示
  - [ ] 实现文件内容显示（代码块样式）
  - [ ] 实现状态图标和动画
  - [ ] 实现错误显示

- [ ] **步骤8：消息列表集成**
  - [ ] 在 `MessageItem` 中判断文件读取工具类型
  - [ ] 渲染文件读取工具调用组件

### 阶段四：安全增强

- [ ] **步骤9：安全机制完善**
  - [ ] 完善路径验证逻辑
  - [ ] 实现二进制文件检测
  - [ ] 实现资源限制
  - [ ] 添加文件大小限制

- [ ] **步骤10：错误处理完善**
  - [ ] 完善文件不存在时的建议功能
  - [ ] 完善权限错误处理
  - [ ] 完善读取失败处理

### 阶段五：测试和优化

- [ ] **步骤11：功能测试**
  - [ ] 测试基本文件读取（文本文件）
  - [ ] 测试大文件读取（分页读取）
  - [ ] 测试图片文件读取
  - [ ] 测试 PDF 文件读取
  - [ ] 测试二进制文件检测
  - [ ] 测试文件不存在时的建议

- [ ] **步骤12：安全测试**
  - [ ] 测试路径限制
  - [ ] 测试二进制文件拦截
  - [ ] 测试资源限制

- [ ] **步骤13：性能优化**
  - [ ] 优化大文件读取性能
  - [ ] 优化内容处理
  - [ ] 优化 UI 渲染

## 十、技术要点总结

### 10.1 路径处理

**核心要点**：
1. 支持绝对路径和相对路径
2. 相对路径相对于项目根目录（`process.cwd()` 或 `userData` 的父目录）
3. 路径安全验证：默认只允许访问用户目录、项目目录和临时目录

### 10.2 文件类型处理

**文本文件**：
- 按行读取和格式化
- 支持行号显示（cat -n 格式）
- 支持分页读取（offset + limit）

**图片文件**：
- 返回 base64 编码的 data URL
- 通过 attachments 传递
- 支持常见图片格式（PNG, JPEG, GIF, WebP, SVG）

**PDF 文件**：
- 返回 base64 编码的 data URL
- 通过 attachments 传递

**二进制文件**：
- 检测并拒绝读取
- 返回友好的错误提示

### 10.3 Function Calling 结果返回格式

参考 terminal tool 的实现，使用 `function_call_output` 类型返回工具调用结果（Responses API 格式）。

### 10.4 文件不存在时的建议

当文件不存在时，读取目录内容，查找相似的文件名，提供建议列表。

## 十一、注意事项

### 11.1 安全性

1. **路径限制**：严格限制可访问的路径，防止访问系统文件
2. **二进制文件检测**：检测并拒绝读取二进制文件
3. **资源限制**：限制读取行数和单行长度，防止内存溢出
4. **文件大小限制**：建议添加文件大小限制（如 10MB）

### 11.2 错误处理

1. **文件不存在**：提供相似文件名建议
2. **权限错误**：处理权限不足的情况，返回清晰的错误提示
3. **读取失败**：捕获错误并返回友好的错误信息

### 11.3 用户体验

1. **实时反馈**：显示文件读取状态（执行中、完成、失败）
2. **内容格式化**：使用代码块样式显示文件内容
3. **长文件处理**：支持分页读取，提示是否还有更多内容
4. **图片显示**：支持图片文件的预览显示

### 11.4 兼容性

1. **跨平台支持**：考虑 Windows、macOS、Linux 的路径差异
2. **编码处理**：正确处理不同编码的文件（默认 UTF-8）
3. **路径格式**：正确处理不同操作系统的路径格式

## 十二、扩展功能（未来）

### 12.1 文件搜索

- 支持文件名模糊搜索
- 支持文件内容搜索

### 12.2 文件编辑

- 支持文件写入功能
- 支持文件追加功能

### 12.3 目录浏览

- 支持列出目录内容
- 支持递归列出子目录

### 12.4 文件监控

- 支持文件变更监控
- 支持文件删除检测

## 十三、参考资料

### 13.1 相关文档

- [09.TERMINAL_TOOL_DESIGN.md](./09.TERMINAL_TOOL_DESIGN.md) - 终端工具设计（参考实现）
- [07.TOOL_CALL_DISPLAY_DESIGN.md](./07.TOOL_CALL_DISPLAY_DESIGN.md) - 工具调用显示设计
- [06.OPENAI_RESPONSES_API_DESIGN.md](./06.OPENAI_RESPONSES_API_DESIGN.md) - Responses API 集成
- [00.AI_CHAT_DESIGN.md](./00.AI_CHAT_DESIGN.md) - AI 聊天功能设计

### 13.2 参考实现

- `/Users/double/projects/opencode/packages/opencode/src/tool/read.ts` - 参考实现
- `/Users/double/projects/opencode/packages/opencode/src/tool/read.txt` - 工具描述参考

### 13.3 API 参考

- OpenAI Function Calling 文档
- Node.js fs 模块文档
- Electron 安全最佳实践

---

**文档创建时间**: 2025-01-XX  
**设计版本**: 1.0.0

