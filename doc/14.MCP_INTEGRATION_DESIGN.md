# MCP 集成设计方案

## 一、背景

### 1.1 功能目标

为项目增加 MCP (Model Context Protocol) 功能，让 AI 支持调用 MCP 服务器提供的工具和资源。MCP 是一个开放标准，允许 AI 应用连接外部数据源和工具。

### 1.2 核心需求

- **固定配置**：MCP 服务器地址和配置写死在代码中，不需要动态配置
- **HTTP 模式**：仅支持 HTTP 传输协议（不支持 stdio、SSE 等）
- **本地服务器支持**：必须支持本地 HTTP MCP 服务器（如 `http://localhost:3000/mcp`）
- **工具调用**：AI 可以调用 MCP 服务器提供的工具
- **资源访问**：AI 可以访问 MCP 服务器提供的资源

### 1.3 技术选型

#### 使用 @modelcontextprotocol/sdk（官方 SDK）

**优点**：
- **官方 SDK**：MCP 官方提供的 TypeScript SDK，稳定可靠
- **直接支持 HTTP 模式**：提供 `HttpClientTransport` 用于连接 HTTP MCP 服务器
- **无需代理服务器**：可以直接连接本地服务器（`http://localhost:3000/mcp`）
- **标准协议实现**：完全符合 MCP 协议规范
- **完善的 API**：提供工具列表获取、工具调用、资源访问等完整功能

**缺点**：
- 需要引入新的依赖 `@modelcontextprotocol/sdk`
- 需要将 MCP 工具转换为 Function Calling 格式以集成到 OpenAI Responses API

**结论**：**使用 @modelcontextprotocol/sdk（官方 SDK）**

## 二、@modelcontextprotocol/sdk 库分析

### 2.1 库功能

`@modelcontextprotocol/sdk` 是 MCP 官方提供的 TypeScript SDK，支持：
- MCP 客户端和服务器实现
- HTTP、SSE、stdio 等多种传输方式
- 工具调用和资源访问
- 流式响应处理

### 2.2 HTTP 客户端支持

SDK 提供了 `HttpClientTransport` 类，专门用于连接 HTTP MCP 服务器：

```typescript
import { Client } from '@modelcontextprotocol/sdk/client/index.js'
import { HttpClientTransport } from '@modelcontextprotocol/sdk/client/http.js'

const transport = new HttpClientTransport({
  url: 'http://localhost:3000/mcp'
})

const client = new Client({
  name: 'ai-client',
  version: '1.0.0'
}, {
  capabilities: {}
})

await client.connect(transport)
```

### 2.3 与当前架构的兼容性

**关键问题**：当前项目使用 OpenAI Responses API 的流式聊天，需要将 MCP 工具转换为 Function Calling 格式。

**分析**：
1. **API 差异**：
   - Responses API：`client.responses.create()` - 流式响应，使用 Function Calling
   - MCP SDK：`client.callTool()` - MCP 协议工具调用

2. **集成方式**：
   - **保持现有架构**：继续使用 Responses API 进行流式聊天
   - **MCP 客户端**：使用 `@modelcontextprotocol/sdk` 连接到本地 MCP 服务器
   - **工具转换**：将 MCP 工具转换为 Function Calling 格式，添加到 Responses API 的工具列表
   - **工具调用**：当 AI 调用 MCP 工具时，通过 MCP 客户端执行，然后将结果返回给 Responses API

## 三、架构设计（使用 @modelcontextprotocol/sdk）

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│              Renderer 层 (UI)                           │
│  - 聊天界面（无需修改）                                  │
└───────────────────────┬─────────────────────────────────┘
                        │ IPC 通信
┌───────────────────────▼─────────────────────────────────┐
│              IPC 层 (通信桥梁)                          │
│  - 现有 IPC 通道（无需修改）                            │
└───────────────────────┬─────────────────────────────────┘
                        │ IPC 调用
┌───────────────────────▼─────────────────────────────────┐
│              Main Handler 层                            │
│  - AIHandler（无需修改）                                │
└───────────────────────┬─────────────────────────────────┘
                        │ 调用
┌───────────────────────▼─────────────────────────────────┐
│              Provider 层 (AI 提供商)                    │
│  - OpenAIProvider                                      │
│    └─ 使用 @modelcontextprotocol/sdk 连接本地 MCP 服务器│
│    └─ 获取 MCP 工具列表                                │
│    └─ 转换为 Function Calling 格式                    │
│    └─ 添加到 Responses API 工具列表                    │
│    └─ 处理 MCP 工具调用（通过 MCP SDK）               │
└───────────────────────┬─────────────────────────────────┘
                        │ 本地连接（无需代理）
┌───────────────────────▼─────────────────────────────────┐
│          @modelcontextprotocol/sdk MCP 客户端            │
│  - Client + HttpClientTransport                        │
│  - 直接连接 localhost:3000/mcp                        │
│  - 获取工具列表和调用工具                              │
└───────────────────────┬─────────────────────────────────┘
                        │ HTTP 请求（本地）
┌───────────────────────▼─────────────────────────────────┐
│          本地 MCP 服务器                                │
│  - http://localhost:3000/mcp                            │
│  - 提供工具和资源                                       │
└─────────────────────────────────────────────────────────┘
```

**关键变化**：
- **移除代理服务器**：不需要代理服务器和 ngrok
- **使用 @modelcontextprotocol/sdk**：官方 SDK 处理本地 MCP 连接
- **工具转换**：将 MCP 工具转换为 Function Calling 格式
- **混合架构**：Responses API 用于聊天，MCP SDK 用于 MCP 工具调用

### 3.2 设计模式

- **适配器模式**：将 MCP 工具适配为 Function Calling 格式
- **桥接模式**：桥接 Responses API 和 MCP SDK
- **策略模式**：根据工具类型选择不同的调用策略

## 四、目录结构（使用 @modelcontextprotocol/sdk）

```
src/
├── main/
│   ├── providers/
│   │   ├── tools/
│   │   │   ├── terminal-tool.ts          # 现有终端工具
│   │   │   ├── read-tool.ts               # 现有文件读取工具
│   │   │   └── mcp-config.ts              # 新增：MCP 工具配置（固定配置）
│   │   ├── mcp/                           # 新增：MCP 客户端模块（使用 @modelcontextprotocol/sdk）
│   │   │   ├── index.ts                    # MCP 客户端导出
│   │   │   ├── client.ts                   # MCP 客户端实现（使用 MCP SDK）
│   │   │   ├── tool-adapter.ts             # MCP 工具适配器（转换为 Function Calling）
│   │   │   └── types.ts                    # 类型定义
│   │   └── openai-provider.ts             # 修改：集成 MCP 工具
│   └── handlers/
│       └── ai-handler.ts                  # 无需修改
├── types/
│   └── chat-frontend-type.ts              # 修改：扩展 ToolType（可选）
└── doc/
    └── 14.MCP_INTEGRATION_DESIGN.md        # 本文档
```

**关键变化**：
- **新增 MCP 客户端模块**：使用 `@modelcontextprotocol/sdk` 实现
- **工具适配器**：将 MCP 工具转换为 Function Calling 格式
- **移除代理服务器**：不需要 `mcp-proxy/` 目录

## 五、核心接口设计

### 5.1 MCP 工具配置（固定配置）

```typescript
// src/main/providers/tools/mcp-config.ts

/**
 * MCP 工具固定配置
 * 注意：这是写死的配置，不支持动态配置
 */
export const MCP_TOOL_CONFIG = {
  // 是否启用 MCP 工具
  enabled: true,
  
  // MCP 工具配置列表（可以配置多个 MCP 服务器）
  tools: [
    // 示例：本地 MCP 服务器
    {
      type: 'mcp' as const,
      server_label: 'time-mcp',
      server_description: 'Local MCP server running on localhost:3000',
      server_url: 'http://localhost:3000/mcp',  // 本地服务器地址（直接支持）
      require_approval: 'never' as const
    },
    // 示例：远程 MCP 服务器
    // {
    //   type: 'mcp' as const,
    //   server_label: 'remote_mcp',
    //   server_description: 'Remote MCP server description',
    //   server_url: 'https://mcp-server.example.com/mcp',
    //   require_approval: 'never' as const
    // }
  ]
} as const

/**
 * 获取启用的 MCP 工具配置
 */
export function getMcpToolConfigs() {
  if (!MCP_TOOL_CONFIG.enabled) {
    return []
  }
  return MCP_TOOL_CONFIG.tools
}
```

### 5.2 MCP 客户端实现（使用 @modelcontextprotocol/sdk）

```typescript
// src/main/providers/mcp/client.ts

import { Client } from '@modelcontextprotocol/sdk/client/index.js'
import { HttpClientTransport } from '@modelcontextprotocol/sdk/client/http.js'
import { logInfo, logError, logDebug } from '../../utils/logger'
import { getMcpToolConfigs } from '../tools/mcp-config'

/**
 * MCP 客户端（使用 @modelcontextprotocol/sdk）
 */
export class McpClient {
  private clients: Map<string, Client> = new Map()
  private toolsCache: Map<string, any[]> = new Map()
  
  /**
   * 初始化所有 MCP 服务器连接
   */
  async initialize(): Promise<void> {
    const configs = getMcpToolConfigs()
    
    for (const config of configs) {
      try {
        // 创建 HTTP 传输层
        const transport = new HttpClientTransport({
          url: config.server_url
        })
        
        // 创建 MCP 客户端
        const client = new Client(
          {
            name: 'ai-client',
            version: '1.0.0'
          },
          {
            capabilities: {
              tools: {}
            }
          }
        )
        
        // 连接到 MCP 服务器
        await client.connect(transport)
        
        this.clients.set(config.server_label, client)
        
        // 获取工具列表
        const tools = await this.getToolsFromServer(client)
        this.toolsCache.set(config.server_label, tools)
        
        logInfo('MCP server connected', {
          label: config.server_label,
          url: config.server_url,
          toolsCount: tools.length
        })
      } catch (error) {
        logError('Failed to connect to MCP server', {
          label: config.server_label,
          url: config.server_url,
          error
        })
      }
    }
  }
  
  /**
   * 从 MCP 服务器获取工具列表
   */
  private async getToolsFromServer(client: Client): Promise<any[]> {
    try {
      const response = await client.listTools()
      return response.tools || []
    } catch (error) {
      logError('Failed to list tools from MCP server', { error })
      return []
    }
  }
  
  /**
   * 调用 MCP 工具
   */
  async callTool(serverLabel: string, toolName: string, arguments: Record<string, any>): Promise<any> {
    const client = this.clients.get(serverLabel)
    if (!client) {
      throw new Error(`MCP server not found: ${serverLabel}`)
    }
    
    logDebug('Calling MCP tool', { serverLabel, toolName, arguments })
    
    try {
      // 调用 MCP 工具
      const result = await client.callTool({
        name: toolName,
        arguments
      })
      
      // 格式化结果：MCP 工具返回的是 CallToolResult，包含 content 数组
      // 需要将 content 转换为字符串
      if (result.content && Array.isArray(result.content)) {
        return result.content
          .map((item: any) => {
            if (item.type === 'text') {
              return item.text
            } else if (item.type === 'image') {
              // 图片类型，返回 data URL 或路径
              return item.data || item.url || ''
            }
            return JSON.stringify(item)
          })
          .join('\n')
      }
      
      return JSON.stringify(result, null, 2)
    } catch (error) {
      logError('MCP tool call failed', {
        serverLabel,
        toolName,
        error
      })
      throw error
    }
  }
  
  /**
   * 获取所有工具（转换为 Function Calling 格式）
   */
  getToolsAsFunctionCalling(): Array<{
    type: 'function'
    name: string
    description: string
    parameters: any
  }> {
    const tools: any[] = []
    
    for (const [serverLabel, serverTools] of this.toolsCache.entries()) {
      for (const tool of serverTools) {
        // MCP 工具的 inputSchema 是 JSON Schema 格式
        // 需要转换为 OpenAI Function Calling 的 parameters 格式
        const parameters = this.convertMcpSchemaToOpenAI(tool.inputSchema)
        
        tools.push({
          type: 'function' as const,
          name: `mcp_${serverLabel}_${tool.name}`,  // 添加前缀避免冲突
          description: tool.description || '',
          parameters
        })
      }
    }
    
    return tools
  }
  
  /**
   * 将 MCP JSON Schema 转换为 OpenAI Function Calling 的 parameters 格式
   */
  private convertMcpSchemaToOpenAI(schema: any): any {
    if (!schema || typeof schema !== 'object') {
      return {
        type: 'object',
        properties: {},
        required: []
      }
    }
    
    // MCP 的 inputSchema 已经是 JSON Schema 格式
    // OpenAI Function Calling 的 parameters 也是 JSON Schema 格式
    // 基本可以直接使用，但需要确保格式正确
    return {
      type: schema.type || 'object',
      properties: schema.properties || {},
      required: schema.required || []
    }
  }
  
  /**
   * 停止所有 MCP 服务器连接
   */
  async disconnect(): Promise<void> {
    for (const [label, client] of this.clients.entries()) {
      try {
        await client.close()
        logInfo('MCP server disconnected', { label })
      } catch (error) {
        logError('Failed to disconnect MCP server', { label, error })
      }
    }
    this.clients.clear()
    this.toolsCache.clear()
  }
}
```

### 5.3 OpenAI Provider 集成

```typescript
// src/main/providers/openai-provider.ts

import { McpClient } from './mcp/client'
import { getMcpToolConfigs } from './tools/mcp-config'

// MCP 客户端单例
let mcpClient: McpClient | null = null

// 初始化 MCP 客户端（在应用启动时调用）
export async function initializeMcpClient(): Promise<void> {
  if (getMcpToolConfigs().length === 0) {
    return
  }
  
  mcpClient = new McpClient()
  await mcpClient.initialize()
}

// 在 streamChatWithResponsesAPI 方法中
private async streamChatWithResponsesAPI(
  messages: AIMessageInput[],
  config: AIConfig,
  callbacks: StreamCallbacks,
  abortSignal?: AbortSignal,
  tools: ToolType[] = [],
  options?: {
    tools?: ToolType[]
  }
): Promise<void> {
  // ... 现有代码 ...
  
  // 构建工具列表
  const toolsList: any[] = []
  
  // 添加现有工具
  for (const tool of tools) {
    if (tool === 'web_search') {
      toolsList.push({ type: 'web_search' as const })
    } else if (tool === 'terminal') {
      toolsList.push(terminalToolDefinition)
    } else if (tool === 'read') {
      toolsList.push(readToolDefinition)
    }
  }
  
  // 新增：添加 MCP 工具（转换为 Function Calling 格式）
  if (mcpClient) {
    const mcpTools = mcpClient.getToolsAsFunctionCalling()
    toolsList.push(...mcpTools)
    logDebug('MCP tools added to request', { count: mcpTools.length })
  }
  
  // ... 后续代码 ...
}

// 在 handleFunctionCall 方法中处理 MCP 工具调用
private async handleFunctionCall(
  functionCall: { 
    name: string; 
    arguments: string; 
    callId: string;
    functionCallItem?: any;
  },
  toolInfo: ToolCallInfo,
  // ... 其他参数 ...
): Promise<void> {
  // ... 现有 terminal 和 read 工具处理 ...
  
  // 新增：MCP 工具调用处理
  if (functionCall.name.startsWith('mcp_')) {
    if (!mcpClient) {
      throw new Error('MCP client not initialized')
    }
    
    // 解析工具名称：mcp_{serverLabel}_{toolName}
    const parts = functionCall.name.split('_')
    if (parts.length < 3) {
      throw new Error(`Invalid MCP tool name: ${functionCall.name}`)
    }
    
    const serverLabel = parts[1]
    const toolName = parts.slice(2).join('_')
    const args = JSON.parse(functionCall.arguments)
    
    logDebug('[handleFunctionCall] Calling MCP tool', {
      serverLabel,
      toolName,
      arguments: args
    })
    
    try {
      // 调用 MCP 工具
      const result = await mcpClient.callTool(serverLabel, toolName, args)
      
      // 格式化结果
      const formattedResult = typeof result === 'string' 
        ? result 
        : JSON.stringify(result, null, 2)
      
      // 更新工具调用信息
      const completedToolInfo: ToolCallInfo = {
        ...toolInfo,
        status: 'completed',
        ...(toolInfo.type === 'mcp' ? {
          toolName: functionCall.name,
          arguments: args
        } : {})
      }
      
      callbacks.onToolCallComplete?.(completedToolInfo, formattedResult)
      
      // 构建 function_call_output 消息
      const functionCallOutput = {
        type: 'function_call_output' as const,
        call_id: functionCall.callId,
        output: formattedResult
      }
      
      // 递归调用
      if (functionCall.functionCallItem) {
        messages.push({
          role: 'user',
          content: [
            functionCall.functionCallItem,
            functionCallOutput
          ]
        } as any)
      } else {
        messages.push({
          role: 'user',
          content: functionCallOutput
        } as any)
      }
      
      await this.streamChatWithResponsesAPI(
        messages,
        config,
        callbacks,
        abortSignal,
        options?.tools || []
      )
    } catch (error) {
      const errorMessage = error instanceof Error ? error.message : String(error)
      logError('[handleFunctionCall] MCP tool call failed', {
        toolName: functionCall.name,
        error: errorMessage
      })
      
      const failedToolInfo: ToolCallInfo = {
        ...toolInfo,
        status: 'failed'
      }
      
      callbacks.onError(new Error(`MCP tool call failed: ${errorMessage}`), failedToolInfo)
    }
  }
}
```

## 六、实现步骤（使用 @modelcontextprotocol/sdk）

### 阶段一：安装依赖和基础配置

- [ ] **步骤1：安装 @modelcontextprotocol/sdk**
  - [ ] 1.1 运行 `pnpm add @modelcontextprotocol/sdk`
  - [ ] 1.2 检查版本兼容性

- [ ] **步骤2：创建 MCP 工具配置**
  - [ ] 2.1 创建 `src/main/providers/tools/mcp-config.ts`
  - [ ] 2.2 定义固定配置（本地服务器地址、标签、描述等）

### 阶段二：MCP 客户端实现

- [ ] **步骤3：创建 MCP 客户端**
  - [ ] 3.1 创建 `src/main/providers/mcp/client.ts`
  - [ ] 3.2 实现 `McpClient` 类，使用 `Client` 和 `HttpClientTransport`
  - [ ] 3.3 实现工具列表获取（`client.listTools()`）
  - [ ] 3.4 实现工具调用（`client.callTool()`）
  - [ ] 3.5 实现工具转换为 Function Calling 格式

- [ ] **步骤4：MCP 客户端初始化**
  - [ ] 4.1 在应用启动时初始化 MCP 客户端
  - [ ] 4.2 处理连接错误和重试逻辑
  - [ ] 4.3 在应用退出时断开连接

### 阶段三：集成到 OpenAI Provider

- [ ] **步骤5：修改 OpenAI Provider**
  - [ ] 5.1 在 `openai-provider.ts` 中导入 `McpClient`
  - [ ] 5.2 在工具列表构建时添加 MCP 工具（转换为 Function Calling）
  - [ ] 5.3 在 `handleFunctionCall` 中添加 MCP 工具调用处理

- [ ] **步骤6：处理 MCP 工具调用事件**
  - [ ] 6.1 在 `response.output_item.added` 事件中识别 MCP 工具调用
  - [ ] 6.2 处理 MCP 工具调用完成事件
  - [ ] 6.3 格式化 MCP 工具返回结果（content 数组转换为字符串）

### 阶段四：测试和优化

- [ ] **步骤7：功能测试**
  - [ ] 7.1 测试 MCP 客户端是否能连接到本地服务器
  - [ ] 7.2 测试工具列表获取
  - [ ] 7.3 测试工具调用
  - [ ] 7.4 测试工具转换（Function Calling 格式）
  - [ ] 7.5 测试错误处理（连接失败、工具调用失败等）

- [ ] **步骤8：日志和错误处理**
  - [ ] 8.1 添加详细的日志记录
  - [ ] 8.2 完善错误处理机制
  - [ ] 8.3 添加用户友好的错误提示

## 七、方案对比

| 特性 | 方案 A（代理服务器） | 方案 B（@modelcontextprotocol/sdk） |
|------|---------------------|-------------------------------------|
| **本地 MCP 支持** | ✅ 需要代理服务器 + ngrok | ✅ 直接支持，无需代理 |
| **实现复杂度** | 高（需要代理服务器、ngrok 管理） | 低（使用官方 SDK） |
| **依赖** | 无额外依赖 | 需要 @modelcontextprotocol/sdk |
| **架构改动** | 中等（新增代理层） | 小（新增 MCP 客户端） |
| **维护成本** | 高（需要维护代理服务器） | 低（官方 SDK 维护） |
| **错误处理** | 需要自己实现 | SDK 提供完善的错误处理 |
| **协议兼容性** | 需要自己实现协议 | ✅ 完全符合 MCP 协议 |
| **推荐度** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## 八、注意事项

### 8.1 @modelcontextprotocol/sdk API 确认

**重要**：需要确认 `@modelcontextprotocol/sdk` 的实际 API：

1. **Client 和 HttpClientTransport 的 API**：
   - `client.connect(transport)` 方法的使用方式
   - `client.listTools()` 方法的返回格式
   - `client.callTool()` 方法的参数和返回格式
   - `client.close()` 方法的使用方式

2. **工具格式**：
   - `listTools()` 返回的工具格式是什么？
   - `inputSchema` 的格式（JSON Schema）
   - 如何转换为 Function Calling 格式？

3. **工具调用结果**：
   - `callTool()` 返回的 `CallToolResult` 格式
   - `content` 数组的结构（text、image 等类型）
   - 如何格式化返回结果？

4. **错误处理**：
   - 连接失败时的错误格式
   - 工具调用失败时的错误格式

**建议**：在实际实现前，先查看 `@modelcontextprotocol/sdk` 的官方文档和示例代码。

### 8.2 架构兼容性

- **保持现有架构**：继续使用 Responses API 进行流式聊天
- **MCP 工具转换**：将 MCP 工具转换为 Function Calling 格式
- **混合使用**：Responses API + MCP SDK 客户端

### 8.3 性能考虑

- **连接管理**：MCP 客户端在应用启动时初始化，保持长连接
- **工具缓存**：工具列表在初始化时获取并缓存
- **并发调用**：支持多个 MCP 工具并发调用
- **连接复用**：同一个 MCP 服务器的多个工具调用复用同一个连接

### 8.4 HTTP 传输模式

- **Streamable HTTP**：MCP SDK 的 HTTP 传输支持流式响应
- **本地连接**：可以直接连接 `http://localhost:3000/mcp`，无需代理
- **错误重试**：连接失败时可以实现重试逻辑

## 九、参考资料

- [@modelcontextprotocol/sdk 官方文档](https://github.com/modelcontextprotocol/typescript-sdk)
- [MCP 官方文档](https://modelcontextprotocol.io/)
- [MCP TypeScript SDK 文档](https://opendeep.wiki/modelcontextprotocol/typescript-sdk/)
- [OpenAI Function Calling 文档](https://platform.openai.com/docs/guides/function-calling)
- [OpenAI Responses API 文档](https://platform.openai.com/docs/api-reference/responses)
