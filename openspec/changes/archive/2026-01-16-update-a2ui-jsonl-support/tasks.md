## 1. 修改 A2UI 检测器
- [ ] 1.1 更新 `isA2UIMessage` 函数，支持检测 JSONL 格式（每行一个 JSON 对象）
- [ ] 1.2 更新 `extractA2UIJSON` 函数，将 JSONL 格式转换为 JSON 数组格式
- [ ] 1.3 移除对 JSON 数组格式的支持
- [ ] 1.4 添加 JSONL 格式的验证逻辑（确保每行都是有效的 JSON 对象）

## 2. 实现提前检测消息类型
- [ ] 2.1 在 `ai-handler.ts` 的 `onChunk` 回调中检测 `---BEGIN A2UI---` 标记
- [ ] 2.2 当检测到标记时，立即设置消息的 `contentType = 'a2ui'`
- [ ] 2.3 立即更新数据库中的消息记录，设置 `contentType`
- [ ] 2.4 通知前端消息类型已更新（通过 IPC 事件或消息更新事件）
- [ ] 2.5 进入 A2UI 模式，开始流式解析 JSONL（可选，用于实时渲染）

## 3. 更新规范
- [ ] 3.1 更新 `openspec/specs/skills/spec.md` 中的 A2UI 消息格式描述
- [ ] 3.2 更新场景描述，反映 JSONL 格式支持
- [ ] 3.3 添加提前检测消息类型的场景描述

## 4. 测试
- [ ] 4.1 测试 JSONL 格式的 A2UI 消息能够正确检测和解析
- [ ] 4.2 测试非 A2UI 格式的消息不会被误判（包括 JSON 数组格式）
- [ ] 4.3 测试格式错误的 JSONL 消息能够正确处理
- [ ] 4.4 测试空的 JSONL 消息能够正确处理
- [ ] 4.5 测试在流式输出过程中提前检测 `---BEGIN A2UI---` 标记并设置 `contentType`
- [ ] 4.6 测试前端能够及时收到消息类型更新，提前准备 A2UI 渲染器
